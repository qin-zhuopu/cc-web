# BMad 文档生成进度总账

> **cron 唤醒 / 断点续跑第一步：读本表 → 找第一个未完成的六边形 → 派子 agent。**
> 每个子 agent 完成一个六边形的全套 BMad 文档后，勾掉对应行并填写产出路径 + 结论。

## 执行模型
- 11 个六边形，每个派 1 个 general-purpose 子 agent，顺序加载 4 个 BMad 技能跑全套。
- 串行推进（一次一个）。cron 每 ~18 分钟兜底唤醒，防卡顿/异常。
- 子 agent 产文档前必读：`docs/architecture/context-boundaries.md`（防重叠）+ `hexagonal-decomposition.md`（基线）。

## 44 格进度表（11 六边形 × 4 BMad 阶段）

| 六边形 | product-brief | prd | architecture | epics-stories | 状态 |
|--------|:---:|:---:|:---:|:---:|------|
| SK Shared Kernel | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |
| C7 Provider | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |
| C1 Conversation | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |
| C2 AgentRuntime | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |
| C3 Subagent | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |
| C8 Workspace | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |
| C9 PluginMCP | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |
| C10 Task | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |
| C4 Media | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |
| C6 Channel | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |
| C5 Bridge | ✅ | ✅ | ✅ | ✅ | ✅ 已完成 |

图例：⬜ 未完成 / ✅ 完成 | 📋 待开始 / 🚧 进行中 / ✅ 已完成

## 执行顺序（绞杀者迁移顺序）
SK → C7 → C1 → C2 → C3 → C8 → C9 → C10 → C4 → C6 → C5

## 决策日志
- 2026-07-30 初始化：建 monorepo 骨架、装 BMad、写主拆解 + 边界契约 + 本总账。方案：11 六边形 × 1 子 agent（内部串行 4 BMad 技能）+ cron 兜底。
- 2026-07-30 SK Shared Kernel 完成：产出全套 4 份 BMad 文档于 `docs/contexts/shared-kernel/`——`product-brief.md`（产品简报，7 端口定位/边界/成功标准）、`prd.md`（7 FR + 6 NFR + 12 AC 验收标准）、`architecture.md`（六边形分层，16 类 ErrorCode 枚举、7 端口到函数签名级、NestJS DI 接线、跨上下文契约核对）、`epics-stories.md`（4 史诗 12 故事 + Story→AC 追溯矩阵 + 3 Sprint 排期）。严守 SK 边界：零业务领域概念，端口清单 = ErrorClassifier/Platform/Redactor/TranslationPort/RuntimeLog/Clock/IdGenerator，与边界契约表 C1/C2/C4/C7 的依赖声明对齐。仅产 .md，未写任何代码。
- 2026-07-30 C1 Conversation 完成：产出全套 4 份 BMad 文档于 `docs/contexts/c1-conversation/`——`product-brief.md`（定位/4 类耦合痛点/边界/成功标准 S1–S5/字段归属与流式相位分离风险）、`prd.md`（5 FR + 7 NFR + 13 AC，含语义契约字段表与 source breadcrumb、持久 streamStatus 不伪装实时相位、无假 tokenUsage 0 反假数据）、`architecture.md`（六边形分层，ChatSession/Message 实体、MessageContent 值对象 + 5 类内容块 encode/decode、TitleOrigin/StreamStatus 值对象、4 驱动端口 + 2 出站端口到函数签名级、NestJS Module DI + forwardRef 解 C1↔C2 环、字段归属节、跨上下文契约核对）、`epics-stories.md`（6 史诗 27 故事 + Story→AC 追溯矩阵 + 3 Sprint 排期）。**C1 核心对外端口**：`ManageSessionUseCase`（会话 CRUD + 改名/归档/touch + source 过滤）、`SetSessionTitleUseCase`（setByUser + generateByAi 经 C2.TitleGenerator + origin 状态机 + 降级）、`AppendMessageUseCase`（追加 + touch 会话 + updateStreamStatus）、`GetSessionHistoryUseCase`（getHistory + getPromptView 剔除 render-only 标记）、出站 `SessionRepository`/`MessageRepository`（会话本体列 + 消息 content↔MessageContent 编解码，供 C2/C5 经用例读写）。**持久 streamStatus vs 实时 phase 语义澄清**：`StreamStatus`(streaming/completed/interrupted/error) 是 C1 拥有的持久转录行生命周期；C2 的 `StreamSession.phase`(active→settling→terminal) 是实时流式相位——C1 类型层面不含 phase，切断 CodePilot stop/abort 卡死的误用根因。严守 C1 边界：不含 AI 生成/流式相位/多 Runtime/Provider/MCP/子 agent；标题 AI 生成只 `import type` `C2.TitleGenerator` 不重写；`SK.Clock`/`IdGenerator`(+ 横切 TranslationPort/RuntimeLog) 只引用不重写；运行时/Provider/Codex 寄存字段（sdk_session_id/codex_thread_id/runtime_status 等）不进 C1 领域模型，归各自上下文。仅产 .md，未写任何代码。
- 2026-07-30 C7 ProviderManagement 完成：产出全套 4 份 BMad 文档于 `docs/contexts/c7-provider/`——`product-brief.md`（定位/6 类配置痛点/边界/成功标准 S1–S5/反假数据风险）、`prd.md`（6 FR + 7 NFR + 13 AC，含语义契约字段表与 source breadcrumb、反例 smoke/禁伪 passed）、`architecture.md`（六边形分层，Provider/Protocol/AuthStyle/Finding/RepairAction 领域模型、驱动端口 + 6 出站端口到函数签名级、NestJS Module DI 接线、跨上下文契约核对）、`epics-stories.md`（7 史诗 33 故事 + Story→AC 追溯矩阵 + 3 Sprint 排期）。**C7 核心对外端口**：`ConfigureProviderUseCase`（CRUD + 默认/启用/排序 + 写路径校验 + resolveAuth）、`DiagnoseUseCase`（5 探针 runDiagnosis + 独立 runLiveProbe + listRepairs/applyRepair）、`ProviderRepository`（出站持久化，同时是 C2 只读消费 Provider 配置的入口，落地引用图 `C7.ProviderRepository → C2 消费`）。**5 vs 6 探针语义澄清**：cli/auth/provider/features/network 为秒级配置时 5 探针（契约"5 探针诊断"），live 探针为独立十几秒深度探针（`DiagnoseUseCase.runLiveProbe`），不计入默认 5 探针集合。严守 C7 边界：不含 AI 调用/流式/会话/MCP；真实 spawn 锁在 `LiveProbeRunner` 适配器后；`SK.ErrorClassifier`（+ 横切 Redactor/RuntimeLog/Clock/TranslationPort）只引用不重写；Features 探针对 stale sdk_session_id 仅经 `SettingsPort` 只读探测不写 C1。仅产 .md，未写任何代码。
- 2026-07-30 C2 AgentRuntime 完成：产出全套 4 份 BMad 文档于 `docs/contexts/c2-agent-runtime/`——`product-brief.md`（定位/5 类实时状态耦合痛点含 #578/边界/成功标准 S1–S6/持久 vs 实时相位分离 + Codex 进程僵死 + C1↔C2 环风险）、`prd.md`（7 FR + 8 NFR + 15 AC，含语义契约字段表与 source breadcrumb、phase 不落库/不与 C1 StreamStatus 混用、ABORTED 与真实错误可分、无假 tokenUsage 0 反假数据）、`architecture.md`（六边形分层，StreamPhase 状态机 + StreamSession 聚合根 + canAccept + abort 不变量、AgentStreamEvent 14 类 + TurnArtifacts/buildFinalContent、TerminalReason 归因、驱动端口 StartStream/AbortStream/TitleGenerator + 出站 AgentRuntimePort 到函数签名级、AbortStreamService force-abort 先行编排、三适配器 + EventMapper 到 Codex 进程隔离细节、NestJS Module DI + 双侧 forwardRef 解 C1↔C2 环、终态→C1 StreamStatus 映射、跨上下文契约核对）、`epics-stories.md`（7 史诗 40 故事 + Story→AC 追溯矩阵 + 3 Sprint 排期）。**C2 核心对外端口**：`StartStreamUseCase`（发起回合 + Runtime 选择 + getPromptView 历史 + 单 active 约束 + 事件消费 + 落 C1）、`AbortStreamUseCase`（force-abort 无条件先行 + reconcilePhase 收敛 + 关 turn）、`AgentRuntimePort`（run/interrupt/forceKillTurn/availability，供 C3 复用，落地引用图 `C2.AgentRuntimePort ← C3`）、`TitleGenerator`（非流式一次性，供 C1，落地 `C2.TitleGenerator ← C1`）。**phase 状态机如何解决 abort 卡死**：`StreamPhase`(active→settling→terminal{completed/aborted/errored}) 是 StreamSession 聚合根的领域不变量——phase 只能经领域方法 `markSettling`/`complete`/`abort`/`fail` 迁移（外部不可直接赋值），abort 方法幂等且强制翻 terminal 子态，**任意 abort 路径后 phase 绝不停 active**（FR-1.4 类型级切断而非人工纪律）；`canAccept()` ≡ `phase !== active` 收敛成唯一领域方法，取代散落的 `phase==='active'` 比较（切断 #578 复合发送排队卡死）；`AbortStreamService` 把 force-abort 安全网**无条件先行**（不排在 interrupt 的 `.finally` 后——这正是 #578 根因：interrupt 挂起 → `.finally` 不跑 → 永不 abort → phase 停 active），复现 interrupt 永不 resolve 时 phase 仍经 force-abort 翻 `terminal(aborted)`、`canAccept()` 立刻 true（AC-2 核心反例回归）。严守 C2 边界：不含会话/消息持久化（经 C1 用例，不直写 messages/chat_sessions）、子 agent 编排（C3 复用 AgentRuntimePort）、Provider 配置管理（只读消费 C7.ProviderRepository）、权限经纪判定（只中转 permission 事件/决议，经纪归 C5）；持久 StreamStatus 不在 C2 建模，只经 `C1.AppendMessageUseCase.updateStreamStatus` 把终态映射写回，phase 是内存态不落库、不与 C1 StreamStatus 混用；`SK.ErrorClassifier`(含 ABORTED 独立类，区分"我停的"vs 真实错误) + 横切 Clock/IdGenerator/RuntimeLog/TranslationPort 只引用不重写；三 Runtime（ClaudeSDK Query+lockId / Native HTTP SSE / Codex app-server 子进程）差异各锁在 `ClaudeSdkRuntimeAdapter`/`NativeRuntimeAdapter`/`CodexRuntimeAdapter` + 各自 EventMapper 内，Codex 的 binary 选最新/`.cmd` shim 经 cmd.exe/fatal config stderr 不等 30s 快失败/僵死 kill/orphan 规避/中断关 thread-turn 全隔离，`@anthropic-ai/*`/`child_process`/`codex`/`better-sqlite3`/`@nestjs/*` 对核心包 0 命中；C1↔C2 环经双侧 NestJS forwardRef 在接线层解，核心包只单向 import type。仅产 .md，未写任何代码。

- 2026-07-30 C8 Workspace 完成：产出全套 4 份 BMad 文档于 `docs/contexts/c8-workspace/`——`product-brief.md`（定位/浏览器读不到本地文件+路径穿越+大文件二进制+预览语义 4 类痛点/边界/成功标准 S1–S6/本机 Web 应用直读磁盘 vs 远程 SaaS 的结构性优势/TOCTOU 局限如实标注）、`prd.md`（4 FR + 7 NFR + 14 AC，含语义契约字段表与 source breadcrumb、截断文件 `truncated=true`&`line_count_exact=false` 反假红线、路径穿越/符号链接逃逸/根+受保护目录三类安全反例、大文件不读流/二进制不返回乱码）、`architecture.md`（六边形分层，FileTreeNode/ScanOptions/FilePreview 值对象 + getLineCap/looksBinary/getFileLanguage/路径安全纯函数、驱动端口 BrowseFilesUseCase(browseTree/previewFile) + 出站端口 FileSystemPort(resolveSafe/readDir/stat/sampleHead/readLines/pathSep) 到函数签名级、路径安全归属决策"判定纯函数留核心 / resolve+realpath 进适配器"、NodeFsAdapter 本机直读 + RemoteFsAdapter 预留、NestJS Module DI 接线、跨上下文契约核对）、`epics-stories.md`（6 史诗 30 故事 + Story→AC 追溯矩阵 + 3 Sprint 排期）。**C8 核心对外端口**：`BrowseFilesUseCase`（browseTree 递归扫描+忽略规则+目录优先排序+深度限制+不可访问子目录优雅降级；previewFile 大文件 stat 拦截+二进制采样拦截+按扩展名行数上限+流式截断读取+诚实行数语义）、`FileSystemPort`（出站唯一 I/O 出口 resolveSafe/readDir/stat/sampleHead/readLines/pathSep，由 NodeFsAdapter 本机直读实现，预留 RemoteFsAdapter 换适配器不动核心；因 C8 无业务上下文消费该端口故不 export，区别于 C7.ProviderRepository）。**本机 Web 应用关键点**：NodeFsAdapter 之所以能直读用户本地磁盘，正因 NestJS 后端跑本机 localhost 拥有完整文件权限——这是"本机 Web 应用"相对"远程 SaaS"读不到本地磁盘的结构性优势。**路径安全归属决策**：判定纯函数（isWithinBase/isRootPath/isBlockedPath/isValidFilename/looksBinary）留核心可测、吃已归一字符串零 fs 依赖；真正 path.resolve/fs.realpath 符号链接逃逸防护（base+target 双 realpath，防文本路径在内真实目标在外、且不误伤 baseDir 本身是符号链接）锁 NodeFsAdapter.resolveSafe 后；TOCTOU 竞态局限如实标注不夸大。严守 C8 边界：只拥有文件树浏览+文件预览，**不含"文件如何被 AI 使用"**（prompt 注入/@文件引用/AI 工具文件读写属 C2）、不含会话/消息（C1）/MCP-Skill（C9）/媒体生成业务语义（C4，生成图在 C8 眼里只是普通文件）；对**业务上下文依赖为无**（最独立上下文之一），仅横切用 SK.Redactor/ErrorClassifier/RuntimeLog/TranslationPort 只引用不重写；写操作不进 C8 浏览/预览核心，只保留路径安全策略作可复用不变量（PRD §7 可选写扩展）；`fs`/`path`/`os`/`readline`/`better-sqlite3`/`@nestjs/*` 对核心包 0 命中（AC-12）。仅产 .md，未写任何代码。

- 2026-07-30 C9 PluginMCP 完成（**重派**：上一个 agent 因 API 卡顿未产出任何文件，本次重新执行并完整产出）：产出全套 4 份 BMad 文档于 `docs/contexts/c9-plugin-mcp/`——`product-brief.md`（定位/配置多来源合并+凭据占位符+自动批准策略模糊+Skill 三语义混淆 4 类痛点/边界/成功标准 S1–S6/编排归属红线：只注册加载不调用编排）、`prd.md`（5 FR + 7 NFR + 16 AC，含 §0 语义契约：Skill 三语义表 availableSkills/loadedSkillBody/调用结果分属 C9/C9/C2、MCP enabled/connectionStatus/toolCount/credentialResolved/autoApprove/externalMcpPresent 来源 breadcrumb 与降级、凭据真实值永不作可见字段、fail-closed 不看前缀）、`architecture.md`（六边形分层，McpServerConfig/McpServerRegistration/ConnectionStatus/McpToolDescriptor + AutoApprovePolicy/ExternalMcpStatus + SkillDescriptor(无 body)/LoadedSkill(正文)/SkillDefinition 三视图类型分离 + splitFrontmatter/parseSkillDefinition/substituteSkillArgs/summarizeExternalMcp/computeAutoApprove 纯函数、驱动端口 RegisterMcpServerUseCase/LoadSkillUseCase/GetRunSupplyUseCase + 出站端口 McpServerPort/SkillLoaderPort/CredentialResolverPort 到函数签名级、归属决策"解析/策略纯函数留核心 / 文件+MCP SDK+DB 进适配器"、NodeMcpAdapter/FsSkillAdapter/DbCredentialResolver + 凭据仅 DbCredentialResolver→resolveServersForRun→C2 交付路径出现、NestJS Module DI + C2 跨 Module 消费两出站端口 CredentialResolverPort 不导出、跨上下文契约核对）、`epics-stories.md`（7 史诗 30 故事 + Story→AC 追溯矩阵 + 3 Sprint 排期）。**C9 核心对外端口**：`RegisterMcpServerUseCase`（register/remove/setEnabled/listAll + probe 注册期能力探测 + describeAutoApprove 策略信号）、`McpServerPort`（出站，readRegistrations 三来源合并+overrides+source / resolveServersForRun 仅启用+凭据解析供 C2 / probe 连接+listTools / probeConfigSources，由 NodeMcpAdapter 实现，需 export 供 C2 消费）、`SkillLoaderPort`（出站，discoverRaw/readSkillRaw/invalidate 只吐原文解析在核心，由 FsSkillAdapter 实现，需 export 供 C2）；补充 `LoadSkillUseCase`（listAvailable 描述视图 / loadBody 正文视图 / invalidate）、`GetRunSupplyUseCase`（getRunSupply 聚合供 C2 一次性取本轮供给）、内部不导出 `CredentialResolverPort`（凭据敏感）。**反假数据核心（Skill 三语义类型层分离）**：`SkillDescriptor`（描述，**刻意不含 body**）≠ `LoadedSkill`（正文 prompt+fork+allowedTools），"实际调用结果"**无 C9 类型**（属 C2 执行事件）；AC-11 反例断言 availableSkills 计数恒定 vs loadBody 只在触发路径返回正文（普通 loaded=0 / 触发 loaded=1）。**凭据不外泄**：`${...}` 真实值只在 DbCredentialResolver→resolveServersForRun→C2 交付路径出现，不进 McpServerRegistration 可见字段（只暴露 credentialResolved/credentialKeysMissing）、不进 SK.RuntimeLog、不进 HTTP 响应（NFR-2/AC-2/16）。**自动批准 fail-closed**：承接现有 external-mcp.ts review round #4/#5——unreadable→undetectable→present、禁止名字前缀信任（codepilot-vault 不豁免）、无来源不 autoApprove。严守 C9 边界：**不含"MCP 工具被 AI 调用的编排"**（无 callMcpTool 运行时调用、无 canUseTool 放行、无 Skill 正文注入对话/起 fork 子 agent，那属 C2/C3；probe 只做注册期能力探测≠调用，AC-6/13）、不做运行时权限放行（只产策略信号供 C5/C2 消费）、不含会话/消息（C1）/Provider auth（C7，MCP ${...} 读通用设置项两条线）/文件浏览（C8）/子 agent 编排（C3）；`SK.RuntimeLog/Redactor/ErrorClassifier/TranslationPort/IdGenerator` 只引用不重写；`fs`/`path`/`os`/`@modelcontextprotocol/sdk`/`better-sqlite3`/`@nestjs/*` 对核心包 0 命中（AC-14）。承接现有 mcp-loader.ts/mcp-connection-manager.ts/skill-discovery.ts/skill-parser.ts/skill-executor.ts/permission/external-mcp.ts。仅产 .md，未写任何代码。

- 2026-07-30 C10 Task 完成：产出全套 4 份 BMad 文档于 `docs/contexts/c10-task/`——`product-brief.md`（定位/瞬时快照不落地就丢+全量覆盖语义易同步错+AI 任务与用户任务混淆+状态语义要诚实 4 类痛点/边界/成功标准 S1–S6/关键分工线"任务由谁产生属 C2、C10 只接收同步"）、`prd.md`（5 FR + 7 NFR + 13 AC，含 §0 语义契约：title/status/description/source/顺序/id/updatedAt/清单整体来源 breadcrumb 与降级、状态未知降 pending 不降 completed 反假红线、替换式全量只删 source='sdk' 不误删用户任务红线）、`architecture.md`（六边形分层，Task 实体/TaskStatus+mapSdkStatus/TaskSource/TodoSnapshot+sanitizeSnapshot/deriveSdkTaskId/TaskError 纯函数与值对象、驱动端口 SyncTasksUseCase(syncFromTodos/listBySession)+ManageUserTaskUseCase + 出站端口 TaskRepository(replaceSdkTasks/listBySession/findById/insertUserTask/updateTask/deleteTask) 到函数签名级、同步语义决策"替换式全量+单事务+source 隔离+稳定派生 id+状态映射诚实"、SqliteTaskRepository 承接现有 syncSdkTasks 事务体 + 内存假端口可替换、NestJS Module DI + C2 进程内消费 SyncTasksUseCase 单向无环、跨上下文契约核对）、`epics-stories.md`（7 史诗 27 故事 + Story→AC 追溯矩阵 + 3 Sprint 排期）。**C10 核心对外端口**：`SyncTasksUseCase`（syncFromTodos 替换式全量同步一轮 TodoWrite 快照+source 隔离+空快照清空+未知状态降 pending 记日志+幂等回传列表；listBySession 按 sort_order 排序读取会话任务）、`TaskRepository`（出站唯一持久化出口 replaceSdkTasks 单事务原子/listBySession/findById/insertUserTask/updateTask/deleteTask，由 SqliteTaskRepository 直写 tasks 表实现，预留换存储不动核心；需 export 供 C2 处理 TodoWrite 后消费 SyncTasksUseCase）；补充 `ManageUserTaskUseCase`（用户手建任务 create/update/remove + source_immutable 校验防用户改动被下轮同步覆盖）。**关键分工线（C10 不含"任务由谁产生"）**：TodoWrite 工具调用如何从 AI 流式回合解析、AI 何时发 todo 属 C2；C2 解析后构造 `TodoSnapshot` 主动调用 `SyncTasksUseCase.syncFromTodos`，C10 是被调用方不反向订阅 AgentStreamEvent——C2→C10 单向无环（与 C7.ProviderRepository 被 C2 消费同构）。**同步语义决策**：TodoWrite 是全量覆盖语义（每轮重发整份清单），故用替换式全量（删该会话所有 sdk 任务+全量重插）而非增量 upsert，最简且天然幂等；必须单事务原子（避免并发读到删了旧的没插新的空清单）；source 隔离硬红线——删除语句严格 `WHERE source='sdk'`，漏掉会连用户手建任务一起清空；稳定派生 id `sdk-${sessionId}-${todoId}` 供前端 diff。**反假数据核心**：`mapSdkStatus` 未知状态（含空串/异常值/未来新增）恒降 `pending` 且回传 `wasUnknown` 记 warn 日志，绝不降 `completed`（现有 syncSdkTasks 静默降级，C10 补上诚实标注）；用户任务同步中原样保留（AC-3 红线反例）。严守 C10 边界：只拥有 TodoWrite 任务项同步，**不含"任务由谁产生"**（AgentStreamEvent 解析/AI 流式相位属 C2，C10 不订阅事件流）、不含会话/消息（C1，只持 session_id 归属键，会话删靠外键 CASCADE）、不含子 agent run/attempt（C3）、不含定时任务调度（scheduled_tasks 另一套机制）；对**业务上下文依赖为无**（相对独立小上下文），仅横切用 SK.Clock/IdGenerator/RuntimeLog/ErrorClassifier/TranslationPort 只引用不重写；`better-sqlite3`/`@nestjs/*`/`@anthropic-ai/*` 对核心包 0 命中且无 AgentStreamEvent import（AC-12）。承接现有 db.ts 的 tasks 表 + syncSdkTasks/getTasksBySession/createTask/updateTask/deleteTask + types/index.ts 的 TaskItem/TaskStatus。仅产 .md，未写任何代码。

- 2026-07-30 C4 MediaGeneration 完成：产出全套 4 份 BMad 文档于 `docs/contexts/c4-media/`——`product-brief.md`（定位/领域逻辑与 SDK-磁盘-DB 纠缠+多 provider 分叉散落+批量执行器状态机脆弱+上下文事件语义模糊+元数据真实性 5 类痛点/边界/成功标准 S1–S6）、`prd.md`（5 FR + 8 NFR + 16 AC，含 §0 语义契约：provider/family/model/status/localPath/elapsedMs/imageCount/Job 计数/synced_at 来源 breadcrumb 与降级，「不把请求参数当实测、不把默认 provider 当真实 provider、失败不显假成图、计数经聚合非估算」反假红线）、`architecture.md`（六边形分层，图片尺寸领域规则 computeGptImage2Size/mapAspectToOpenAISize/parseRatio/detectFamily 纯函数从 image-generator.ts 剥离、MediaJob/MediaJobItem 聚合根 + canTransitionJob/canTransitionItem 状态机 + retry-policy、BatchExecutor 编排取代 globalThis 单例+setTimeout 轮询、驱动端口 GenerateImage/ImportMedia/RunBatchJob/ManageMedia/MediaContextEvent + 出站端口 ImageGeneratorPort/MediaRepository/MediaStoragePort/SchedulerPort/ImageProviderResolverPort 到函数签名级、NestJS Module DI + onApplicationBootstrap reconcileOnBoot、跨上下文契约核对）、`epics-stories.md`（7 史诗 30 故事 + Story→AC 追溯矩阵 + 3 Sprint 排期）。**C4 核心对外端口**：`GenerateImageUseCase`（解析 provider→ImageGeneratorPort 生成→elapsed 实测→skipSave 分支→落盘落库记实测 provider/model→失败归类，供 C2/C9 MCP 管线 codepilot_generate_image + 设计 Agent 前端流程调用）、`RunBatchJobUseCase`（createJob 建 Job(planned)+N Item(pending)/startJob 并发消费/pauseJob 不误杀/cancelJob/subscribeProgress，AI 规划属 C2 产出 items 传入不做规划）、`ImageGeneratorPort`（C4 自有多 provider 抽象，GeminiImageAdapter/OpenAiImageAdapter 各带 family，新增 provider 零核心改动经 Router 分发）、`MediaRepository`（出站唯一持久化出口，5 张 media_* 表 + recountJobCounters 聚合计数 + reconcileOnBoot 重启补偿，供 Gallery/Bridge 回灌读取）。**批量任务状态机（本上下文复杂点）**：Job 状态机 draft→planning→planned→running↔paused→completed/cancelled/failed、Item 状态机 pending→processing→completed/failed(可重试)→cancelled，迁移经 canTransitionJob/canTransitionItem 谓词校验非法抛错；BatchExecutor 用注入 SchedulerPort（可控延时不真实等待）取代现有 globalThis 单例+setTimeout 轮询——暂停不误杀在跑项（跑完落 completed 只不拉新）、取消中断在跑项+pending/failed→cancelled、终态判定全 item 终态有完成→completed 全失败→failed、进程重启补偿 running→paused/processing→pending；退避重试 4xx 不重试其余指数退避 retryDelayMs*3^(retry-1) 超 maxRetries 永久 failed。**反假数据核心**：provider/family/model 落库值 = 适配器实际选中/解析值（非请求原值，AC-10 构造请求 A 解析 B 断言落 B）、elapsedMs/imageCount 为实测投影无值隐藏不显假 0、失败记 status='failed' 不返回可渲染 localPath、Job 计数经 recountJobCounters 按 item 实际状态聚合非估算。严守 C4 边界：**不含文本 AI 流**（无 StreamSession/AgentStreamEvent，属 C2）、不含会话/消息持久化（只持 sessionId/messageId 归属键，不写 chat_sessions/messages）、**不含 Provider 配置管理**（现有 pickImageProvider 直查 api_providers 属越界，重构经 ImageProviderResolverPort 只读 C7 解析结果不写 api_providers）、不含批量任务 AI 规划（planner 属 C2）、不含 MCP 工具调用编排（codepilot_generate_image/MEDIA_RESULT_MARKER 如何注入对话属 C2/C9，C4 只提供被调用用例）、不含媒体文件浏览预览（属 C8）；`SK.IdGenerator`（+ 横切 Clock/ErrorClassifier/RuntimeLog/TranslationPort）只 import type 引用不重写；`ai`/`@ai-sdk/*`/`fs`/`path`/`os`/`crypto`/`better-sqlite3`/`@nestjs/*` 对核心包 0 命中（AC-14），SDK/磁盘/DB/id 生成全在适配器或经端口；C4 单向被 C2/C9 消费、单向消费 C7，无跨上下文环（无 forwardRef 需求）。承接现有 image-generator.ts（Gemini/OpenAI-Image 双 family + GPT Image 2 尺寸约束）/job-executor.ts（globalThis 执行器）/db.ts 的 media_generations/media_tags/media_jobs/media_job_items/media_context_events 表 + types/index.ts 的 MediaJob/MediaJobItem/BatchConfig/JobProgressEvent。仅产 .md，未写任何代码。

- 2026-07-30 C6 Channel 完成：产出全套 4 份 BMad 文档于 `docs/contexts/c6-channel/`——`product-brief.md`（定位/渠道能力差异散落+Markdown 直怼渠道 API 破损+流式卡片生命周期泄进编排+新渠道接入面宽 4 类痛点/边界/成功标准 S1–S7/编排归属红线：只合约能力渲染探测不路由投递编排）、`prd.md`（5 FR + 8 NFR + 17 AC，含 §0 语义契约：ChannelCapabilities 5 能力 streaming/threadReply/search/history/reactions 来源 breadcrumb 与语义红线（飞书 search=false/reactions=false）、ProbeResult/渲染结果/分片结果来源与降级、能力必须与实现同源禁止名字前缀猜能力、凭据 appSecret/token 永不作可见字段）、`architecture.md`（六边形分层，ChannelType/ChannelMeta/ChannelCapabilities+capabilitiesInvariant/ProbeResult/CardStreamController/ChannelPlugin<T> 合约 + MarkdownIR+markdownToIR/renderMarkdownWithMarkers/chunkMarkdownIR/sliceSpans + Telegram/飞书/Discord 渠道渲染器纯函数、驱动端口 ProbeChannelUseCase/RenderMessageUseCase + 出站端口 ChannelPluginPort<T> 到函数签名级、归属决策"渲染/判定纯函数留核心 / 渠道 SDK+WS+HTTP 进适配器"、ChannelPluginRegistry/FeishuChannelPlugin/TelegramChannelPlugin 适配器 + 渲染层从 bridge/ 迁入 C6、NestJS Module DI + C5 跨 Module 消费三端口单向无环无 forwardRef、跨上下文契约核对）、`epics-stories.md`（8 史诗 34 故事 + Story→AC 追溯矩阵 + 3 Sprint 排期）。**C6 核心对外端口**：`ChannelPluginPort<T>`（出站，resolve 按 channelType 解析渠道插件实例 / listRegistered，由 ChannelPluginRegistry + 各渠道插件实现，承接现有 registerAdapterFactory + channel-plugin-adapter，需 export 供 C5 消费）、`ProbeChannelUseCase`（驱动，probe 连通性探测一次返回 ProbeResult + ChannelCapabilities / listSupportedChannels，供渠道设置页测试连接 + C5 启动前预检）；补充 `RenderMessageUseCase`（驱动，render 把 Markdown 渲染为渠道原生格式 + 渲染期分片 payloads，供 C5 取渲染结果 + 设置页预览，只产 payloads 不投递）、`CardStreamController`（接口合约 create/update/finalize + 可选 updateToolCalls/setThinking，C6 定义接口 + 飞书实现，时序编排归 C5）。**渠道特定渲染核心（Markdown→IR→渠道格式）**：MarkdownIR（text + styleSpans + linkSpans）渠道无关中间表示一次解析多渠道渲染，渲染器差异只在 options（styleMarkers/escapeText/buildLink）；Telegram HTML 子集 + wrapFileReferencesInHtml 防 README.md 误链成 http + render-first 分片（先按 IR 切渲染后 HTML 超限再二次切）、飞书 hasComplexMarkdown 复杂度分流（代码块/表格→schema 2.0 卡片 buildCardContent、其余→post buildPostContent）；分片 chunkMarkdownIR span 相对块归零不越界。**反假数据核心（能力诚实）**：ChannelCapabilities 与渠道实现同源如实声明（飞书 search=false 因只有本地过滤非 user_access_token 搜索、reactions=false），capabilitiesInvariant 把"streaming=true 却拿不到 CardStreamController"谎报变成可断言违规（AC-3），C5 禁止基于 channelType 名字前缀猜能力。严守 C6 边界：**不含路由/投递编排**（无 route/deliver/retry 方法、无投递顺序保证、无 bridge-manager 生命周期编排、无流式卡片 create/update/finalize 时序编排，那属 C5；probe 只连通性能力探测≠消费/路由，render 只产 payloads 不投递，AC-17）、不做跨渠道权限经纪（只保留渠道插件自身 isAuthorized 渠道配置准入承接 feishu/policy，跨渠道 PermissionBroker + canUseTool 归 C5/C2）、不含会话/消息（C1，consumeOne 只产 InboundMessage 形状接入会话是 C5 conversation-engine）/AI 流式（C2）/Provider（C7）/MCP-Skill（C9）；**对任何业务上下文零依赖**（最独立上下文之一），仅横切用 SK.Redactor/ErrorClassifier/RuntimeLog/TranslationPort 只引用不重写；InboundMessage/OutboundMessage/SendResult/ChannelType 共享消息形状只 import type 引用不重写不拥有路由投递语义；`@larksuiteoapi/*`/Telegram/Discord SDK/`ws`/`fs`/`path`/`os`/`better-sqlite3`/`@nestjs/*` 对核心包 0 命中（AC-14），渠道 SDK 连接/WS/长轮询/HTTP 发送全在渠道插件适配器后；C6 单向被 C5 消费无环。承接现有 channels/types.ts（ChannelPlugin<T>/ChannelCapabilities/ProbeResult/CardStreamController）+ channels/channel-plugin-adapter.ts + channels/feishu/（gateway/inbound/outbound/identity/policy/card-controller/config/types/index 模块化拆分）+ bridge/markdown/（ir/render/feishu/telegram/discord 渲染器，重构从 bridge/ 迁入 C6）。仅产 .md，未写任何代码。

- 2026-07-30 C5 Bridge 完成（**API 中断后主编排层直接补写**：子 agent 先后两次在 grounding/动笔前因 API 卡顿中断，前 2 份 product-brief/prd 已由子 agent 产出，architecture/epics-stories 由主编排层依 prd 端口清单直接手写补齐，避免第三次卡死）：产出全套 4 份 BMad 文档于 `docs/contexts/c5-bridge/`——`product-brief.md`（定位/编排层痛点/边界/成功标准）、`prd.md`（§0 语义契约反假数据表 + 5 FR + 8 NFR + 19 AC）、`architecture.md`（六边形分层，ChannelBinding/OffsetCursor(fetch vs committed 分离)/PermissionLink 状态机/BridgeStatus 实测状态值对象、驱动端口 RouteInboundMessageUseCase/DeliveryPort/PermissionBrokerPort/ManageBridge/ManageBinding + 出站端口 BindingRepository/OffsetRepository/PermissionLinkRepository/DeliveryLogRepository/RateLimiterPort 到函数签名级、入站闭环 + runAdapterLoop at-least-once + 卡片时序编排流程、NestJS Module DI 注入 C6/C1/C2 用例 + exports PermissionBrokerPort 供 C3、跨上下文契约核对）、`epics-stories.md`（6 史诗 31 故事 + Story→AC 追溯矩阵 + 3 Sprint 排期）。**C5 核心对外端口**：`RouteInboundMessageUseCase`（入站路由：解析/建绑定→C6 准入→callback 分流→C2 起回合→C1 落库）、`DeliveryPort`（出站投递：C6 渲染 payloads→顺序/限速/重试/降级/dedup）、`PermissionBrokerPort`（权限经纪，流阻塞期间转 IM 按钮 + 定向决议 + 自动清理，**供 C3 复用**，落地引用图 `C5.PermissionBrokerPort ← C3`）、`ManageBridgeUseCase`/`ManageBindingUseCase`（生命周期 + 绑定 CRUD）。**编排层如何组合 C1/C2/C6**：C5 本身不拥有渠道协议/会话逻辑/AI 相位，而是把三者编排成 "IM 入站 → C6 准入/收发 → C5 路由 → C1 建会话/落库 → C2 起回合拿 AgentStreamEvent → 按 C6 实测 streaming 能力走卡片(C6 CardStreamController，C5 编排时序)或普通分片 → C1 落 AI 回复" 闭环；PermissionBrokerPort 破解流阻塞 deadlock 并导出供 C3 子 agent 复用（C5 不区分调用方是主会话还是子 agent）。**at-least-once 核心**：OffsetCursor 分离 fetchOffset 与 committedOffset，仅 route 处理完成后推进 committed，崩溃从上次 committed 重放靠 dedup 兜底。严守 C5 边界：不含渠道渲染（属 C6，经 RenderMessageUseCase 消费）、会话/消息实体（C1，经用例读写不直写 DB）、StreamPhase/StreamSession（C2，消费 AgentStreamEvent 不重造相位）、Provider 配置（C7）；`@larksuiteoapi/*`/Telegram/Discord SDK/`ws`/`@anthropic-ai/*`/`better-sqlite3`/`@nestjs/*`/`node:*` 对核心包 0 命中（AC-15）；跨上下文只 import type + NestJS DI。仅产 .md，未写任何代码。

## 收尾核对清单（全部完成后执行）
- [x] 44 格全部 ✅
- [x] 每个六边形 4 份文档齐全（11×4=44，逐目录 ls 核对通过）
- [x] 跨上下文端口引用能对上（C3→C2.AgentRuntimePort、C1→C2.TitleGenerator、C3→C5.PermissionBrokerPort、C5→C6.ChannelPluginPort、C2→C7.ProviderRepository 五组两侧文档均命中一致）
- [x] 顶层架构文档 + 文档索引产出（docs/architecture/overview.md + index.md）

## 完工总结
- 2026-07-30 全部 11 六边形 × 4 BMad 文档（44 份）产出完成，收尾核对四项全过，顶层 overview.md + index.md 产出。全程 cron 兜底 + 卡死自愈：C3 一次 API 中断后补跑补齐；C9 一次误判重派后成功；C5 两次子 agent 卡顿后由主编排层直接手写补齐 architecture/epics-stories。任务完成，cron 兜底任务已删除。
