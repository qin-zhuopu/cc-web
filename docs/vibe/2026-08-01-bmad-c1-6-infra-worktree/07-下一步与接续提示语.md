# 07 · 下一步与接续提示语

## 当前 sprint 全景（截至本轮沉淀）

master HEAD `220d711`（c2-5，期间由并行流程推进）。Phase 4：**16/18 epic done**。

| 领域边界 | 完成度 | 状态 |
|---|---|---|
| SK · Shared Kernel | 4/4 done | 全通 |
| C1 · Conversation | **6/6 done** | 含本轮 c1-6，完整收官，端到端可跑 |
| C2 · AgentRuntime | 5/7 done | c2-1~5 done，剩 c2-6 / c2-7 |
| EPIC-ACCEPT | 0/1 backlog | 9 故事全 backlog |

## 剩余工作

- **epic-c2-6** Runtime 适配器（backlog）：范围内 c2-6-1（Claude SDK 适配器 + EventMapper）、c2-6-6（RuntimeRouter）；Native/Codex 共 5 故事 deferred。**第一个要接真 litellm 代理的 epic**——CLAUDE.md 新增的 `apps/api/.env`（litellm 网关 + Jereh-Kimi-K2.6 模型）就是为它准备的。
- **epic-c2-7** 标题服务 + 权限 + DI 接线（backlog，6 故事）：c2-7-1 GenerateTitleService 落地后，**可替换 c1-6 里的 StubTitleGenerator**；c2-7-4 用 forwardRef 接 C1↔C2 环。
- **EPIC-ACCEPT** 验收链路（backlog，9 故事）：SSE hub + 三件套接口 + 断线补发 + CLI 客户端 + 端到端冒烟。

## 推荐落点

**c2-6-1（Claude SDK 适配器）**。理由：它一通，C2 就能真的调模型跑起来；与 C1 已就绪的 HTTP 入口合起来，就是第一个完整可对话的链路。这也是从「纯核心全绿」迈向「真实可用」的关键一跃。注意它需要真 litellm 代理、须人在场验证。

## 遗留与提醒

1. **StubTitleGenerator 待替换**：c1-6 里 TitleGeneratorPort 暂绑抛错 stub，c2-7-1 落地后替换为真实现（SetSessionTitleService 无需改动，只换 ConversationModule 的绑定）。
2. **core exports 已修为 dist**：跑 apps/api 前先 `tsc --build` 产出 core 的 dist。
3. **worktree 里跑完整 `npm install`**：别只装单个包，否则缺 devDependencies。
4. **HTTP 无鉴权**：仅 127.0.0.1，勿暴露公网。
5. **git 环境坑**：Git-Bash 下裸 git 输出不可信，一律 `/mingw64/bin/git`。
6. **注意过期 gitStatus 注入**：上下文里注入的 gitStatus 是会话起点快照，查 git 真实状态必须实时执行命令，勿采信快照。

## 给下一个会话的接续提示语

> 继续推进 codepilot-web 的 Phase 4。当前 master（约 220d711）已完成 16/18 epic：SK 全、C1 全（含 c1-6 基础设施层，真 SQLite + NestJS HTTP 已端到端可跑）、C2 c2-1~5。请开工 **epic-c2-6-1（Claude SDK Runtime 适配器 + EventMapper）**。注意：① 这是第一个接真 litellm 代理的 epic，env 单一真相源是 `apps/api/.env`（含密钥，勿回显/入库），网关 `ANTHROPIC_BASE_URL=https://litellm.jereh.cn`、模型 `Jereh-Kimi-K2.6`；② 落地后可把 c1-6 的 StubTitleGenerator 换成真实现（属 c2-7-1，可一并评估）；③ 建议开 worktree 隔离开发（记得跑完整 npm install），完事合并回 master；④ 跑 apps/api 前先 tsc --build 产出 core 的 dist。裸 git 输出不可信，用 /mingw64/bin/git。
