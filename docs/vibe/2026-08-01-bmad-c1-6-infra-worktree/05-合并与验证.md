# 05 · 合并与验证

## 提交（worktree 内）

- 用 `/mingw64/bin/git status --short` 确认 worktree 状态干净、只有 c1-6 改动、无 c2-4 文件漏入。
- **只 stage 明确的 c1-6 相关路径**（不用 `git add -A`，避免误入 .env 等密钥文件——CLAUDE.md 已说明 apps/api/.env 含密钥且 gitignored）。先 grep 确认无 .env/db/secret 未跟踪文件。
- 提交 `eb0c729`：`feat(c1): 实现 C1-E6 基础设施层接线（epic-c1-6）`，17 文件 +2073/-15，遵循仓库 `feat(c1): ...（epic-xxx）` 风格。

## 合并（master 主仓库）

master 期间前进到 087af84（c2-4 进来了），与 epic-c1-6 分叉，ff 不可能。用户选普通 merge：

- `ExitWorktree(keep)` 退回主仓库。
- `git merge --no-ff epic-c1-6` → **零冲突**，merge commit `062bbdd`。sprint-status.yaml 因两边改不同行区块被 git 自动三方合并，epic-c1-6 与 epic-c2-4 的 done 标记都保留。

## 合并后验证（必做）

合并把两分支代码合到一起，必须重新验证（尤其 c2-4 动了 core/index.ts、c1-6 动了 core/package.json）：

1. **第一次 `npm test` 失败**：typecheck 报 `Cannot find module 'better-sqlite3'`。根因——better-sqlite3 是在 worktree 装的，master 主仓库 node_modules 没有（package.json 声明合并进来了，但包没装）。
2. **根治**：master 主仓库跑 `npm install` 补齐（better-sqlite3 + @nestjs/platform-express 都装上）。
3. **重跑 `npm test` 全绿**：**42 文件、562 测试通过**，typecheck 0 错，守卫扫 85 文件 0 命中。

## 收尾

- 更新 sprint-status.yaml：epic-c1-6 与 c1-6-* 全标 done，last_updated 改 2026-08-01（在 worktree 内提交前已做）。
- 清理所有临时文件（smoke.db、各 *.log）。
- 移除 worktree + 删分支（详见 02）。

## 验证证据（curl 端到端，worktree 内 smoke）

真 SQLite + 真 NestJS HTTP，全链路打通：
1. POST /api/sessions → 返回 UUID（经 SK.IdGenerator）
2. POST /api/sessions/:id/messages → 落库，streamStatus=completed，**无 tokenUsage 字段**（反假数据，没落假 0）
3. GET /api/sessions/:id/messages → content 经 encode/decode 往返完好
4. GET /api/sessions → titleOrigin=user、时间为 epoch 毫秒（INTEGER 直存）
5. DELETE /api/sessions/:id → {ok:true}
6. 删后 GET messages → `[]`（**级联删除生效**，FK ON DELETE CASCADE 真的工作）
7. 删后 GET session → 404
