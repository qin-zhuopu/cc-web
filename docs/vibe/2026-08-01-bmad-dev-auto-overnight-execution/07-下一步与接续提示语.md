# 07 · 下一步与接续提示语

## 当前状态（沉淀时刻）

- **HEAD `b4a8b3e`**（C2-E3），工作区干净、全部入库。
- **12/18 epic 完成**：sk-1/2/3/4、c1-1/2/3/4/5、c2-1/2/3。
- **499 测试全绿**，import 守卫 0 命中。
- 流水线安全停在此断点。用户已说「继续不要停」，但因执行者输出退化 + 用户打断要求先沉淀文档，实际停在此处等接续。

## 剩余工作

### 纯核心（无外部依赖，可继续用 workflow 无人值守推）
- **c2-4** StartStream 用例：选 Runtime / 历史投影 / 事件消费 / 落 C1（c2-4-1~6）。
- **c2-5** AbortStream 用例：force-abort 先行 / reconcile / 关句柄 / 超时归因（c2-5-1~6）。#578 真实 interrupt 永挂的端到端回归在这一层。

### 基础设施层（需真 litellm 代理 + NestJS/SQLite，必须人在场验证）
- **c1-6** ConversationModule（forwardRef）+ Session/Message Controllers + SQLite 适配器 + 消费契约。
- **c2-6** ClaudeSdkRuntimeAdapter + ClaudeSdkEventMapper（含 resume 续接）+ RuntimeRouter（c2-6-1、6-6；Native/Codex 的 6-2~6-5/6-7 已 deferred）。
- **c2-7** TitleGenerator + 权限事件中转 + AgentRuntimeModule 接线（forwardRef 解 C1↔C2 环）+ 终态→C1 StreamStatus 映射。
- **EPIC-ACCEPT** accept-1~9：C7 stub / SSE 广播 / 文件事件日志(seq) / 三件套 REST / Last-Event-ID 断线补发 / CLI 监听 / 端到端 smoke。

## 恢复方式（新会话接续）

1. **读进度**：`_bmad-output/implementation-artifacts/sprint-status.yaml`（哪些 done/backlog）+ `git log --oneline`（12 个 feat 断点）。
2. **读本 vibe 目录**全部 7 个文件了解来龙去脉与铁律。
3. **验证基线**：`cd 项目根 && npm run test`（应 499 绿，守卫 0 命中）。用 `/mingw64/bin/git` 看状态。
4. **继续纯核心 c2-4**：派子代理拆 `epic-c2-4/`（读 c2-agent-runtime 的 architecture/prd/epics-stories，参照已完成 epic 范式）→ 写 `wf-c2-4.mjs`（波次结构 + Merge+Verify + Review）→ 启动 workflow → 独立复跑门禁 → 修评审真缺陷 → 更新 sprint-status → git 提交断点。
5. **基础设施层**：先确认 `apps/api/.env` 的 litellm 配置可达（`ANTHROPIC_BASE_URL=https://litellm.jereh.cn`、`ANTHROPIC_MODEL=Jereh-Kimi-K2.6`），E2E 连真代理时人在场；无法自测的留「待验清单」。

## 接续必守的铁律（详见 05）

- 核心包零框架、import type + .js、禁 phase 进 C1、phase 不落库进 C2、反假数据、术语纪律（勿用「上下文」指代 bounded context）。
- 用 `/mingw64/bin/git` 全路径；项目是独立仓库勿混 dotfiles。
- workflow 子代理继承会话模型（不受 .env 的 ANTHROPIC_MODEL 影响）；.env 只供运行时 SDK/测试读，密钥绝不入库/回显。
- **每步幂等自检**（先查 HEAD/文件状态再动作）+ 哨兵守断点，对抗输出退化。

## 给下一个会话的精确接续提示语（可直接用）

> 继续 CodePilot Web 后端的 bmad sprint。当前 HEAD `b4a8b3e`，12 epic 已完成（SK 全部 + C1 纯核心 c1-1~5 + C2 的 c2-1~3），499 测试全绿。先读 `docs/vibe/2026-08-01-bmad-dev-auto-overnight-execution/` 全部文件了解上下文与铁律，跑 `npm run test`（用 /mingw64/bin/git）确认基线。然后用 workflow 模式继续纯核心 c2-4（StartStream 用例）：子代理拆 epic-c2-4 stories → 写 wf-c2-4.mjs → 启动 → 独立复跑门禁 → 对抗评审修真缺陷 → 更新 sprint-status → 每 epic 一个 git 断点提交。每步幂等自检。到需真 litellm 代理的 c1-6/c2-6/7/accept 时停下等我在场。

## 未解决 / 遗留

- **执行者输出退化**：本轮多次在工具调用间夹带重复字符，未根治，靠幂等自检兜底。接续会话若无此问题会更顺。
- **deferred-work.md** 里累积的 nitpick（守卫升级为 ESLint、SK 占位适配器换生产实现、C1-E4 Clock 死依赖等）待后续专门处理。
- **run-dev-auto.sh** 已加保护但因退休模型不可用；若日后要用 CLI 模式，需先把环境 ANTHROPIC_MODEL 设为可用模型（如 Jereh-Kimi-K2.6）。
