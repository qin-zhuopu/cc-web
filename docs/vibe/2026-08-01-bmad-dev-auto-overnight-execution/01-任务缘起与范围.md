# 01 · 任务缘起与范围

## 用户初始诉求（逐字要点）

用户要求：用 `bmad-quick-dev` 把 CodePilot Web 后端的 **S1 地基**跑通，并实现第一个故事 **sk-1-1**。

**这一轮的真实目的**（用户明说）：验证 **bmad-dev-auto 全自动开发链路能不能走通**。所以脚手架和边界门禁必须真正立起来、`npm run test` 必须能跑。

## 项目背景

- 项目根：`C:\home\14409.JEREH\repo\github.com\op7418\codepilot-web`
- 六边形架构、绞杀者迁移。monorepo 用 npm/pnpm workspaces（`package.json` 声明 `workspaces: apps/*、packages/*`）。
- 术语纪律（CLAUDE.md）：**禁止用「上下文」指代 bounded context**；指代模块用全称（Shared Kernel / Conversation / AgentRuntime）或「领域边界」。全程中文。

## 本轮范围演进

初始范围仅 S1 + sk-1-1，但用户在验证链路可行后逐步扩大，最终指令是「**用 workflow 完成所有的 sprint**」并配了大模型环境变量（见 04）。执行策略随之分两层：

1. **纯核心层**（`npm run test` 可验，无外部依赖）：SK 全部、C1 的 E1-E5、C2 的 E1-E5 —— **无人值守可批量跑**。
2. **基础设施层**（NestJS + SQLite + 真实 litellm 代理、E2E）：C1-E6、C2-E6/E7、EPIC-ACCEPT —— **需真代理、人在场验证，做到能自测边界即停、留待验清单**。

## 本期 sprint 全貌（88 故事 / 18+ epic）

来自 `_bmad-output/implementation-artifacts/sprint-status.yaml`：

- **SK · Shared Kernel**：epic-sk-1（错误分类）/ sk-2（Clock/IdGenerator/Platform 端口）/ sk-3（Redactor/RuntimeLog）/ sk-4（TranslationPort+DI+守卫）
- **C1 · Conversation**：epic-c1-1（领域+端口骨架）/ c1-2（MessageContent 编解码）/ c1-3（会话生命周期用例）/ c1-4（标题状态机）/ c1-5（消息生命周期用例）/ c1-6（NestJS+SQLite 接线）
- **C2 · AgentRuntime**：epic-c2-1（类型/端口骨架）/ c2-2（StreamSession 聚合根+#578）/ c2-3（EventMapper）/ c2-4（StartStream 用例）/ c2-5（AbortStream 用例）/ c2-6（Claude SDK 适配器）/ c2-7（TitleGenerator+DI 接线）
- **EPIC-ACCEPT · 验收链路**：accept-1~9（C7 stub / SSE 广播 / 文件事件日志 / 三件套 REST / 断线补发 / CLI / 端到端 smoke）
- **deferred（本期明确不做）**：sk-4-4（C7 试点）、c2-6-2~6-5/6-7（Native/Codex 适配器）

## 已完成 vs 剩余（沉淀时刻）

- ✅ 完成 12 epic：sk-1/2/3/4、c1-1/2/3/4/5、c2-1/2/3
- ⏳ 剩余：c2-4/5（纯核心）、c1-6 / c2-6/7 / accept-*（基础设施，需真代理）
